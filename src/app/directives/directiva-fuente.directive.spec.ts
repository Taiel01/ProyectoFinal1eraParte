import { DirectivaFuenteDirective } from './directiva-fuente.directive';

describe('DirectivaFuenteDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaFuenteDirective();
    expect(directive).toBeTruthy();
  });
});
